# Missing values

####First draft 1018

Import function plot_patterns(df, percent = TRUE) from the .R file.
```{r, include=FALSE}
library(tidyverse)
library(dplyr)
source("missing_patterns.R", local = knitr::knit_global())
```

Examine the missing values on a subset of the data set. Select the 
```{r}
testtest <- natural[,c(2:12)]
#plot_patterns(testtest, percent = TRUE)
```